name: Qodana

on:
  push:
  pull_request:

jobs:
  qodana:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run Qodana
        uses: JetBrains/qodana-action@v2023.2
        with:
          args: --linter=jetbrains/qodana-jvm-community:2023.2
