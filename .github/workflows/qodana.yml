name: Qodana

on:
  workflow_dispatch:
  #push:
  #pull_request:

jobs:
  qodana:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run Qodana
        uses: JetBrains/qodana-action@v2023.2
